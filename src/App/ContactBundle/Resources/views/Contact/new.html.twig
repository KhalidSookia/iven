{% extends "AppAppBundle::layout.html.twig" %}

{% block body -%}

    <div id="slider_wrapper_n">
        <div class="title">
            <h2>Nous Contacter</h2>
        </div>
        <div class="padding15">
            
        {% for topText in topTexts %}
            <p>{{topText.Text | raw }}</p>
        {% endfor %}
        </div>

    </div>

    <div id="templatemo_main">
    
    <div class="col_w900 col_w900_last">
    
          <div class="col_w420 float_l">
          
              <h3>Formulaire de Contact</h3>
              
              <div id="cp_contact_form">
              
                {{ form_start(form) }} 
                {{ form_row(form.name, {'label': 'Name:'}) }}
                {{ form_row(form.email, {'label': 'Email:'}) }}
                {{ form_row(form.subject, {'label': 'Sujet:'}) }}
                {{ form_row(form.content, {'label': 'Message:'}) }}
                <div class="cleaner_h10"></div>
                            
                {{ form_end(form) }}    
            </div>
      </div>
            
            <div class="col_w420 float_r" id="map">
                <h6>Nous contacter</h6>
                Email: sebastienk@iven-invest.com <br /> 
                Tel: +33 6 14 92 29 28<br />
            </div>
            
            <div class="cleaner"></div>
      </div>
        
        <div class="cleaner"></div>
        
        <span class="bottom"></span>    
    </div> 
    

{% block js %}

{% endblock %}
{% endblock %}
